var Flagger = require('../common/object/flagger');
var stringify = require('../common/json/stringify');

/**
 * Events are bound to a peer, which is an event emitter.
 */
module.exports = Flagger.extend({

  /**
   *
   */
  init: function (ring) {

  }

});
